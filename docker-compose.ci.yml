version: "3.6"

services:
  frontend_app:
    build:
      context: .
      target: production
    ports:
      - "8001:3000"
    networks:
      - togl
    environment:
      NUXT_HOST: "0.0.0.0"

networks:
  togl:
    external:
      name: togl
